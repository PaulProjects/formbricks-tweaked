/**
 * SSO handlers stub.
 *
 * Always returns null since SSO is an EE feature.
 *
 * SPDX-License-Identifier: AGPL-3.0-only
 */

export const handleSsoCallback = async (..._args: any[]): Promise<boolean> => true;
