/**
 * Teams list stub.
 *
 * Always returns empty array since teams are an EE feature.
 *
 * SPDX-License-Identifier: AGPL-3.0-only
 */

export const getOrganizationTeams = async (..._args: any[]): Promise<any[]> => [];
export const getTeam = async (..._args: any[]): Promise<any> => null;
export const getTeamsByOrganizationId = async (..._args: any[]): Promise<any[]> => [];
